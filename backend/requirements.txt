# Runtime dependencies (pinned).
pydantic==2.10.4
pydantic-settings==2.7.1

# Ingestion: PDF -> Markdown
pymupdf4llm==1.28.2
pymupdf==1.28.2

# Storage & retrieval: PostgreSQL + pgvector
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pgvector==0.3.6

# Embeddings client (Ollama HTTP)
httpx==0.28.1

# Reranking: cross-encoder (bge-reranker) via sentence-transformers (pulls torch)
sentence-transformers==3.3.1

# Web API
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Auth & compliance (password hashing, JWT, per-user encryption)
argon2-cffi==23.1.0
PyJWT==2.10.1
cryptography==44.0.0
